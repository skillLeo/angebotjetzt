import { BadgeCheck, Car, ClipboardCheck, Shield, UserRound, Wrench } from 'lucide-vue-next';
import type { Component } from 'vue';

export const avatarIcons: Record<string, Component> = {
    Shield,
    Wrench,
    BadgeCheck,
    Car,
    ClipboardCheck,
    UserRound,
};
